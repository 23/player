{% if supportsVolumeChange %}
<div class="tray-button button volume-button {% if volumeMuted %}volume-button-on highlight-button{% else %}volume-button-off{% endif %}" click="$toggle:volumeMuted" aria-label="{{"toggle_volume"|translate}}" label="{{"toggle_volume"|translate}}" role="button" tabindex="1700">
  {% if volumeMuted %}
  <!--Sound-->
  {% else %}
  <!--Mute-->
  {% endif %}
</div>
{% endif %}
