{% if showBrowse and hasRecommendations %}
<div class="button browse-button {% if browseMode %}browse-button-off{% else %}browse-button-on{% endif %}" click="$toggle:browseMode">
  Browse
</div>
{% endif %}
