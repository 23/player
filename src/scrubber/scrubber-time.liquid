{% if scrubberTime == 0 and currentTime == 0 %}
	{{0|formatTime}} / {{duration|formatTime}}
{% else %}
	{% if scrubberTime != empty %}
	  {{scrubberTime|formatTime}} / {{duration|formatTime}}
	{% else %}
	  {{currentTime|formatTime}} / {{duration|formatTime}}
	{% endif %}
{% endif %}
