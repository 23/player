{% if playing and volumeMuted and mutedAutoPlay and ambient == false %}
  <div class="muted-auto-play muted-auto-play-{{unmuteButtonPosition}}">
    <div click="$toggle:mutedAutoPlay" class="muted-auto-play-button muted-auto-play-button-{{unmuteButtonPosition}}">
      Click to unmute
    </div>
  </div>
{% endif %}
