{% player status-display %}
{% player core %}
{% player analytics %}
{% player playflow %}

<div id="player">
  <!-- Video display -->
  {% player video-display with "displayDevice":"flash" %}

  <div class="big-button tray-button">{% player big-play-button %}</div>

  {% player subtitles %}

  <!-- Control bar or tray -->
  <div id="tray">
    {% player info %}
    {% player sharing %}

    <div class="tray-navigation">
      <ul class="tray-left">
        {% player play-button %}
      </ul>

      <div class="tray-right">
        {% player fullscreen-button %}
        {% player volume-button %}
        {% player subtitle-button %}
        {% player quality-button %}
        {% player share-button %}
        {% player info-button %}
        {% player browse-button %}
      </div>

      <div class="tray-scrubber">
        <div class="tray-scrubber-sub">
          {% player scrubber %}
          {% player sections %}
        </div>
      </div>
    </div>
    {% player browse %}
  </div>

  {% player logo %}
</div>



