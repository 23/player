{% if supportsFullscreen %}
  <div class="tray-button button fullscreen-button {% if fullscreen %}fullscreen-button-off{% else %}fullscreen-button-on{% endif %}" click="$toggle:fullscreen">
    {% if fullscreen == true %}
    !FS
    {% else %}
    FS
    {% endif %}
  </div>
{% endif %}
