{% if supportsFullscreen %}
  <div class="tray-button button fullscreen-button {% if fullscreen %}fullscreen-button-off{% else %}fullscreen-button-on{% endif %}" click="$toggle:fullscreen" aria-label="{{"toggle_fullscreen"|translate}}" label="{{"toggle_fullscreen"|translate}}" role="button" tabindex="1800">
    {% if fullscreen == true %}
    <!--Not full screen-->
    {% else %}
    <!--Full screen-->
    {% endif %}
  </div>
{% endif %}
